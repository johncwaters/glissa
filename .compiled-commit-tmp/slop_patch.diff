--- a/session/sessions.js
+++ b/session/sessions.js
@@ -754,7 +754,7 @@
       waitMs,
       active: activeAgents,
       heldSeq: held.seq,
       lastActivitySeq: this._lastActivitySeq,
-      quietMs: now - Math.max(this._gateQuietSince, held.ts),
+      quietMs: now - held.ts,
     });
     if (decision === "cancel") {
