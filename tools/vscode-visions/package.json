{
  "name": "glissa-visions",
  "displayName": "Glissa Visions",
  "publisher": "johnwaters",
  "version": "0.3.0",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Glissa Visions",
      "properties": {
        "glissaVisions.relayPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to session/visions-relay.js. Empty uses the path stamped at install time."
        },
        "glissaVisions.port": {
          "type": "number",
          "default": 0,
          "description": "Relay port. Use 0 to let the relay choose its fallback."
        }
      }
    }
  },
  "description": "Mirrors markdown buffers to the Glissa daemon and shows its findings and silent fixes."
}
