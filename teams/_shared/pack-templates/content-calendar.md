# Content Calendar

<!-- GLISSA:NEEDS-INPUT: replace everything below with this project's real calendar, then save. -->

The topics this team draws from. The Researcher reads this first and picks one fresh topic per run,
skipping anything used in recent runs (it checks log.md).

You can list topics directly here, or point to where the calendar lives in this repo (for example a
path or a query). Each entry should carry enough context for the Researcher to judge fit: the theme,
the intended audience, and any timing.
